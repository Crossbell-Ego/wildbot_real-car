## Linux 使用截圖

```bash
lameshot gui
```

這兩個腳本的主要差別在於 **「開發調試」** 與 **「正式運行」** 的用途不同：

### 1. `sudo ./launch_shell.sh` (開發與調試用)

```jsx
cd "/home/robot/wildbot_real car/wildbot_workspace-main" && sudo sudo ./launch_shell.sh
```

- **互動性**：它會啟動一個容器並讓你進入終端機（bash）。你可以在裡面手動輸入指令、編譯程式碼 (`colcon build`)。
- **單一目的**：它通常只啟動一個名為 `wildbot` 的主容器，並將你目前的 `workspaces` 資料夾掛載進去。
- **手動控制**：雷達、相機等節點不會自動執行，需要你進去後手動 `ros2 launch`。
- **用途**：當你要**測試新驅動**（像我們剛才在做的）、寫程式、或是需要觀察單一節點的輸出時使用。

### 2. `sudo ./scripts/00_start_all.sh` (正式全機運行用)

```jsx
cd "/home/robot/wildbot_real car/wildbot_workspace-main" &&  sudo ./scripts/00_start_all.sh

```

### 上面的執行後要在別的終端機開啟，要用以下指令

```bash
docker exec -it compose-kros_car-1 bash
```

- **自動化**：它會利用 `docker-compose` 一口氣啟動機器人運作所需的所有服務（如：底盤控制、相機、雷達、網頁橋接器等）。
- **多容器**：它會同時啟動多個不同的容器，每個容器各司其職。
- **背景執行**：通常節點會在背景自動執行，你不需要（也不會）進入終端機。
- **用途**：當機器人已經設定完成，你要**讓整台機器人動起來**、準備出去跑任務時使用。

---

### 簡單總結：

- 如果你想**修程式碼**或**測試新雷達** $\rightarrow$ 用 **`launch_shell.sh`**。
- 如果你想**直接開車**或**運行整套系統** $\rightarrow$ 用 **`00_start_all.sh`**。

我們剛才安裝 YDLIDAR 屬於「開發測試」階段，所以必須先在 `launch_shell.sh` 裡確認可以成功驅動。等測試成功後，我們再把 YDLIDAR 的啟動指令寫進 `docker-compose` 裡，這樣以後用 `00_start_all.sh` 就能自動開啟它了！

---

## 3. slam 建圖

**1. 啟動機器人基礎服務（底盤、相機、雷達）**：

```bash
cd "/home/robot/wildbot_real car/wildbot_workspace-main" &&  sudo ./scripts/00_start_all.sh
```

**2. 當您想要開始建圖時，進入容器並執行 `slam.sh`**：

```bash
docker exec -it compose-kros_car-1 bash
```

1. **開始建圖**

```bash
./slam.sh
```

這樣一來，平常不建圖時就不會佔用系統資源，只有在您需要時才會開啟 SLAM 功能！

---

## 4. YOLO 3D 與 自動抓取任務 (Grab Task)

當您想要進行 AI 物體辨識並讓機械手臂自動抓取目標物時：

**1. 啟動機器人基礎服務**：

```bash
cd "/home/robot/wildbot_real car/wildbot_workspace-main" &&  sudo ./scripts/00_start_all.sh
```

**2. 進入容器並啟動 YOLO 3D 偵測**：

```bash
docker exec -it compose-kros_car-1 bash
# 進入後執行
./yolo_3d.sh
```

**3. 另開一個終端機進入容器，執行自動抓取任務**：

```bash
docker exec -it compose-kros_car-1 bash
# 進入後執行抓取腳本
python3 workspaces/src/arm_ik/arm_ik/grab_task.py
```

*註：`grab_task.py` 會訂閱 `/yolo/detections_3d` 並根據辨識到的物體位置自動計算逆向運動學 (IK) 並控制手臂。*

---

## 5. 手臂位置教導與執行 (Arm Teach & Execute)

當您需要精確紀錄手臂位置，並在未來需要重複執行特定動作序列時使用：

**1. 進入教導目錄**：
進入容器後，切換至手臂腳本存放路徑：
```bash
docker exec -it compose-kros_car-1 bash
cd /workspaces/workspaces/src/arm_ik/arm_ik/
```

**2. 啟動教導程式 (錄製與測試點位)**：
搭配手把將手臂移至目標點後，使用鍵盤操作：
```bash
python3 grab_teach.py
```
*   **數字鍵 0-9**：紀錄目前位置（自動存檔至 `arm_poses.json`）。
*   **R 鍵**：切換模式。在「回放模式」下，按數字鍵手臂會自動移動到該點。
*   **I 鍵**：顯示目前夾爪的實時空間座標 (X, Z cm)。

**3. 執行自動化點位序列**：
直接在終端機輸入編號即可自動依序執行：
```bash
# 範例：依序移動到點位 1 -> 2 -> 1

cd /workspaces/workspaces/src/arm_ik/arm_ik && source /workspaces/install/setup.bash && python3 grab_execute.py

python3 grab_execute.py 1 2 1
```

---

## 6. 手把操控與安全即停 (Teleop & E-Stop)

為了確保操作安全，系統配備了「一鍵即停」機制：

| 功能              | 按鍵 (Xbox/ROG) | 說明                                                |
| :---------------- | :-------------- | :-------------------------------------------------- |
| **緊急即停/解鎖** | **L1 (LB)**     | **切換式 (Toggle)**。按一下鎖定整機，再按一下解除。 |
| **底盤移動**      | 左搖桿          | 上下控制前進後退，左右控制旋轉。                    |
| **手臂第一節**    | Y / A           | Y 往上，A 往下。                                    |
| **手臂第二節**    | X / B           | X 往上，B 往下。                                    |
| **夾爪控制**      | L2 / R2         | L2 打開，R2 閉合。                                  |

**安全提醒：**
- 當進入 **LOCKED** 狀態時，終端機會顯示紅字警告，此時所有控制無效。
- 當顯示 **UNLOCKED** 黃字提示後，才可繼續操作。
- 建議在測試新指令（如 `move.py`）前，先確認 L1 處於解鎖狀態。