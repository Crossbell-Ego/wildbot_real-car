# 🚀 Wildbot 快捷指令集 (一鍵捷徑)

為了避免每次都要輸入冗長的 `docker exec` 或 `cd` 尋找路徑的指令，我們建立了一個**快捷指令集**。

### 設定步驟：
1. 在主機端開啟 `~/.bashrc`：
   ```bash
   nano ~/.bashrc
   ```
2. 在檔案最底部加入這行以載入捷徑：
   ```bash
   source "/home/robot/wildbot_real car/wildbot_workspace-main/scripts/wildbot_aliases.sh"
   ```
3. 存檔退出後，更新終端機設定：
   ```bash
   source ~/.bashrc
   ```

### 快捷指令對照表：

> [!TIP]
> **💡 一鍵直達免進容器**：因為以下指令（如 `teach`、`nav`、`lcoord` 等）在底層已經**用 `&&` 自動串好了進入容器、載入環境與執行腳本的動作**，所以您在日常測試時，**完全不需要先進入容器**，直接在主機終端機輸入快捷指令即可！

| 快捷指令       | 實際執行行為 / 對應原指令                                  | 說明                                         |
| :------------- | :--------------------------------------------------------- | :------------------------------------------- |
| **`start`**    | 啟動底盤、相機、雷達等所有硬體驅動服務                     | `sudo ./scripts/00_start_all.sh`             |
| **`enter`**    | 進入容器的主目錄 (`/workspaces`)                           | `docker exec -it compose-kros_car-1 bash`    |
| **`nstart`**   | 啟動 Nav2 導航與 YOLO 3D 視覺 (以 n 開頭防與 start 衝突)   | `./quick_start.sh`                           |
| **`nstop`**    | 停止所有背景導航與視覺服務                                 | `./quick_start.sh stop`                      |
| **`teleop`**   | 啟動手把遙控器程式                                         | `./teleop.sh`                                |
| **`slam`**     | 啟動 **SLAM 建圖**與手把背景遙控                           | `./slam.sh`                                  |
| **`savemap`**  | 一鍵**儲存建圖地圖**（產出於 `maps/my_map`）               | `ros2 run nav2_map_server map_saver_cli ...` |
| **`initpose`** | 自動發布初始定位原點 `(0, 0)`                              | `ros2 topic pub -1 /initialpose ...`         |
| **`lcoord`**   | 啟動**光達版**任務協調器（支援傳參，如 `lcoord 0.5 0.38`） | `python3 lidar_competition_coordinator.py`   |
| **`coord`**    | 啟動 Nav2 版任務協調器                                     | `python3 competition_coordinator.py`         |
| **`nav`**      | 執行導航任務（支援多點，如 `nav 1 2`）                     | `python3 nav_execute.py`                     |
| **`arm`**      | 執行手臂動作（支援多點，如 `arm 1`                         | `python3 grab_execute.py`                    |
| **`teach`**    | 啟動手臂點位教導程式                                       | `python3 grab_teach.py`                      |
| **`nteach`**   | 啟動**導航點位**教導/回放程式                              | `python3 nav_teach.py`                       |
| **`grab`**     | 啟動相機自動對齊與夾取測試                                 | `python3 "camera and grab.py"`               |
| **`calib`**    | 啟動相機與機械手臂校正量測工具                             | `python3 calibration_tool.py`                |
| **`bnav`**     | 啟動**自動過橋**任務（支援傳參，如 `bnav L` 或 `bnav R`）  | `python3 bridge_navigation.py`               |

---



docker exec -it compose-kros_car-1 bash
source /workspaces/install/setup.bash
python3 /workspaces/test_foxglove_lidar.py





## Linux 使用截圖

要在電腦終端機

flameshot gui


這兩個腳本的主要差別在於 **「開發調試」** 與 **「正式運行」** 的用途不同：

### 1. `sudo ./launch_shell.sh` (開發與調試用)

```jsx
cd "/home/robot/wildbot_real car/wildbot_workspace-main" && sudo sudo ./launch_shell.sh
```

- **互動性**：它會啟動一個容器並讓你進入終端機（bash）。你可以在裡面手動輸入指令、編譯程式碼 (`colcon build`)。
- **單一目的**：它通常只啟動一個名為 `wildbot` 的主容器，並將你目前的 `workspaces` 資料夾掛載進去。
- **手動控制**：雷達、相機等節點不會自動執行，需要你進去後手動 `ros2 launch`。
- **用途**：當你要**測試新驅動**（像我們剛才在做的）、寫程式、或是需要觀察單一節點的輸出時使用。

### 2. `sudo ./scripts/00_start_all.sh` (正式全機運行用)

```jsx
cd "/home/robot/wildbot_real car/wildbot_workspace-main" &&  sudo ./scripts/00_start_all.sh

```

### 上面的執行後要在別的終端機開啟，要用以下指令

```bash
docker exec -it compose-kros_car-1 bash
```

- **自動化**：它會利用 `docker-compose` 一口氣啟動機器人運作所需的所有服務（如：底盤控制、相機、雷達、網頁橋接器等）。
- **多容器**：它會同時啟動多個不同的容器，每個容器各司其職。
- **背景執行**：通常節點會在背景自動執行，你不需要（也不會）進入終端機。
- **用途**：當機器人已經設定完成，你要**讓整台機器人動起來**、準備出去跑任務時使用。

---

### 簡單總結：

- 如果你想**修程式碼**或**測試新雷達** $\rightarrow$ 用 **`launch_shell.sh`**。
- 如果你想**直接開車**或**運行整套系統** $\rightarrow$ 用 **`00_start_all.sh`**。

我們剛才安裝 YDLIDAR 屬於「開發測試」階段，所以必須先在 `launch_shell.sh` 裡確認可以成功驅動。等測試成功後，我們再把 YDLIDAR 的啟動指令寫進 `docker-compose` 裡，這樣以後用 `00_start_all.sh` 就能自動開啟它了！

---

## 3. SLAM 建圖與地圖儲存 (SLAM Mapping & Save Map)

本章節說明如何啟動 SLAM 建圖、使用遊戲手把遙控小車，以及在建圖完成後如何儲存地圖。

### 🚀 第一步：啟動 SLAM 建圖與手把遙控
在實體主機終端機，直接輸入：
```bash
slam
```
*(在底層，這等同於進入容器並執行 `./slam.sh`。此指令會自動在背景啟動手把遙控，並在前景啟動 SLAM，按 `Ctrl + C` 結束時會自動清理後台遙控進程。)*

---

### 🎨 第二步：在 RViz 中觀察建圖
在操作電腦上啟動 **RViz2**：
1. 將 **Fixed Frame** 設定為 `map`。
2. 新增 **Map** 顯示項，將話題 (Topic) 設定為 `/map`。
3. 新增 **LaserScan** 顯示項，將話題 (Topic) 設定為 `/scan`。
4. 使用手把遙控小車在場地內**緩慢移動**（特別是轉彎時要慢，否則雷達特徵容易匹配失敗），直到地圖完整建立。

---

### 💾 第三步：儲存地圖
當您在 RViz 中確認地圖建立完畢後，請**不要關閉建圖的 `slam` 視窗**。
另開一個實體主機終端機視窗，直接輸入：
```bash
savemap
```
*(在底層，這等同於一鍵在容器內調用 `map_saver_cli` 將地圖存檔至主機的 `maps` 資料夾中，並附帶 10.0 秒防逾時機制。)*

如果您需要使用 **SLAM Toolbox 內建服務**進行儲存（以便後續載入繼續建圖）：
```bash
docker exec -it compose-kros_car-1 bash -c "source /workspaces/install/setup.bash && ros2 service call /slam_toolbox/save_map slam_toolbox/srv/SaveMap \"{name: {data: '/workspaces/maps/my_map'}}\""
```

儲存成功後，您會在 `maps/` 資料夾下看到兩個新檔案：
* `my_map.pgm`（地圖畫素圖檔）
* `my_map.yaml`（地圖設定檔，供 Nav2 導航載入使用）

---

## 4. YOLO 3D 與 自動抓取任務 (Grab Task)

當您想要進行 AI 物體辨識並讓機械手臂自動抓取目標物時：

**1. 啟動機器人基礎服務**：

```bash
cd "/home/robot/wildbot_real car/wildbot_workspace-main" &&  sudo ./scripts/00_start_all.sh
```

**2. 進入容器並啟動 YOLO 3D 偵測**：

```bash
docker exec -it compose-kros_car-1 bash
# 進入後執行
./yolo_3d.sh
```

**3. 另開一個終端機進入容器，執行自動抓取任務**：

```bash
docker exec -it compose-kros_car-1 bash
# 進入後執行抓取腳本
python3 workspaces/src/arm_ik/arm_ik/grab_task.py
```

*註：`grab_task.py` 會訂閱 `/yolo/detections_3d` 並根據辨識到的物體位置自動計算逆向運動學 (IK) 並控制手臂。*

---

## 5. 手臂位置教導與執行 (Arm Teach & Execute)

當您需要精確紀錄手臂位置，並在未來需要重複執行特定動作序列時使用：

**1. 進入教導目錄**：
進入容器後，直接定位至手臂腳本存放路徑：
```bash
docker exec -it -w /workspaces/workspaces/src/arm_ik/arm_ik/ compose-kros_car-1 bash
```

**2. 啟動教導程式 (錄製與測試點位)**：
搭配手把將手臂移至目標點後，使用鍵盤操作：
```bash
python3 grab_teach.py
```
*   **數字鍵 0-9**：紀錄目前位置（自動存檔至 `arm_poses.json`）。
*   **R 鍵**：切換模式。在「回放模式」下，按數字鍵手臂會自動移動到該點。
*   **I 鍵**：顯示目前夾爪的實時空間座標 (X, Z cm)。

**3. 執行自動化點位序列**：
直接在終端機輸入編號即可自動依序執行：
```bash
# 範例：依序移動到點位 1 -> 2 -> 1

cd /workspaces/workspaces/src/arm_ik/arm_ik && source /workspaces/install/setup.bash && python3 grab_execute.py

python3 grab_execute.py 1 2 1
```

---

## 6. 手把操控與安全即停 (Teleop & E-Stop)

**啟動手把控制：**

先確認已經啟動機器人基礎服務：

```bash
cd "/home/robot/wildbot_real car/wildbot_workspace-main" && sudo ./scripts/00_start_all.sh
```

另開一個終端機，進入 `kros_car` 容器：

```bash
docker exec -it compose-kros_car-1 bash
```

進入容器後執行：

```bash
cd /workspaces
./teleop.sh
```
./slam.sh

`teleop.sh` 會自動啟動：

```bash
ros2 run joy joy_node --ros-args -p autorepeat_rate:=20.0
python3 /workspaces/joy_to_base_camera.py
```

為了確保操作安全，系統配備了「一鍵即停」機制：

| 功能              | 按鍵 (Xbox/ROG) | 說明                                                |
| :---------------- | :-------------- | :-------------------------------------------------- |
| **緊急即停/解鎖** | **L1 (LB)**     | **切換式 (Toggle)**。按一下鎖定整機，再按一下解除。 |
| **底盤移動**      | 左搖桿          | 上下控制前進後退，左右控制旋轉。                    |
| **前後車速微調**  | 十字鍵 上 / 下  | 按「上」鍵加速 (每次 +0.05m/s)，按「下」鍵減速。    |
| **自轉速度微調**  | 十字鍵 左 / 右  | 按「左」鍵加速 (每次 +0.05rad/s)，按「右」鍵減速。  |
| **手臂第一節**    | Y / A           | Y 往上，A 往下。                                    |
| **手臂第二節**    | X / B           | X 往上，B 往下。                                    |
| **夾爪控制**      | L2 / R2         | L2 打開，R2 閉合。                                  |

**安全提醒：**
- 當進入 **LOCKED** 狀態時，終端機會顯示紅字警告，此時所有控制無效。
- 當顯示 **UNLOCKED** 黃字提示後，才可繼續操作。
- 建議在測試新指令（如 `move.py`）前，先確認 L1 處於解鎖狀態。
- 如果手把沒有反應，先確認容器內是否看得到 `/joy`：

```bash
ros2 topic echo /joy --once
```

### **如何監控手把的按鍵與搖桿編號？**

在容器內執行以下指令，可以實時印出目前手把的所有輸入訊號：

```bash
ros2 topic echo /joy
```

**如何對應按鍵與編號：**

1. **`buttons: [...]` 陣列**：
   * 代表手把上的**普通按鍵**（如 A, B, X, Y, L1, R1, Start, Select 等）。
   * 沒按時數值為 `0`，按下時數值變為 `1`。
   * **如何判斷編號**：在 `buttons` 陣列中，從左到右為索引 `0, 1, 2, 3...`。例如：按下 A 鍵時如果陣列中第 1 個數字（索引 0）變成 1，代表 A 鍵編號是 `0`。

2. **`axes: [...]` 陣列**：
   * 代表手把上的**類比搖桿與板機鍵**（如左右搖桿的上下左右、L2, R2 的按壓深淺）。
   * 數值通常介於 `-1.0 ~ 1.0` 之間。
   * **如何判斷編號**：在 `axes` 陣列中，從左到右為索引 `0, 1, 2, 3...`。例如：將左搖桿推到最上方時，如果第 2 個數字（索引 1）變為 `1.0`，代表左搖桿上下方向的編號是 `1`。

---

## 7. 載入地圖與 Nav2 導航 (Nav2 Navigation)

當您已經使用 SLAM 完成建圖並儲存地圖後，可以使用 `nav2.sh` 啟動導航，此系統已同步進行底層 DDS 與 Action 升級，確保導航的極致穩定性。

**1. 啟動機器人基礎服務（若尚未啟動）**：
```bash
cd "/home/robot/wildbot_real car/wildbot_workspace-main" && sudo ./scripts/00_start_all.sh
```

**2. 進入容器並啟動 Nav2 導航**：
```bash
docker exec -it compose-kros_car-1 bash
```
您有兩種啟動方式（推薦方式一）：

*   **【方式一】使用快捷腳本啟動（推薦）**：
    ```bash
    # 載入已儲存的地圖進行導航
    ./nav2.sh /workspaces/maps/my_map.yaml
    
    # 或是邊建圖邊導航 (SLAM + Nav2)
    ./nav2.sh slam
    ```
*   **【方式二】使用 ROS2 Launch 原生指令啟動**：
    ```bash
    ros2 launch wildbot_nav2 nav2_launch.py map:=/workspaces/maps/my_map.yaml
    ```

**3. 在 Foxglove Studio / RViz 中操作**：
1. **設定初始定位 (2D Pose Estimate) 📍**：
   在 Foxglove 畫面頂部點選 `2D Pose Estimate`，在地圖上找到小車的實際物理位置，**按住滑鼠左鍵拉出車頭朝向的箭頭**後放開。AMCL 將自動對齊雷達點雲與牆壁。
2. **發送導航目的地 (Nav Goal) 🏁**：
   點選 `Nav Goal`（或 `2D Goal Pose`），在地圖上您的目的地**按住滑鼠左鍵拉出小車到達後的朝向箭頭**。小車即會自動規劃黃色/綠色路徑並開始避障移動！

---

### 🛠️ 導航常見故障診斷與排除

#### 🔴 問題一：點選導航目標後小車完全不動，日誌顯示 Action 被拒絕/中斷
*   **原因**：後台有重複的導航進程在互相衝突，或是定位未初始化。
*   **解決方法**：
    1. 進入容器：`docker exec -it compose-kros_car-1 bash`
    2. 強制終止所有殘留的導航進程：
       ```bash
       pkill -f nav2
       pkill -f component_container_isolated
       ```
    3. 重新執行啟動指令，並確保在發送導航點前有先設定 **2D Pose Estimate** 初始定位。

#### 🔴 問題二：地圖加載失敗，日誌提示找不到 yaml
*   **原因**：地圖路徑錯誤，或地圖檔案名稱寫錯。
*   **解決方法**：
    1. 確認 `/workspaces/maps/` 下是否有 `my_map.yaml` 與 `my_map.pgm`。
    2. 使用原生指令時，必須傳入絕對路徑 `/workspaces/maps/my_map.yaml`。

---

## 8. 精確距離移動控制 (Move by Specific Distance)

當您需要讓小車移動特定距離（例如前進 5cm 或後退 10cm）時，可使用里程計反饋控制腳本 `move_distance.py`：

**1. 確保已啟動機器人基礎服務**：
```bash
cd "/home/robot/wildbot_real car/wildbot_workspace-main" && sudo ./scripts/00_start_all.sh
```

**2. 進入容器執行移動腳本**：
```bash
docker exec -it compose-kros_car-1 bash
```

**3. 執行移動指令（參數單位為公尺）**：
*   **前進 5 cm (0.05m)**：
    ```bash
    python3 move_distance.py 0.05
    ```
*   **後退 10 cm (-0.1m)**：
    ```bash
    python3 move_distance.py -0.1
    ```
*   **指定速度前進（例如前進 5 cm，速度 0.1 m/s）**：
    ```bash
    python3 move_distance.py 0.05 0.1
    ```
*   **草地/打滑地形補償移動（例如在草地上想移動 30 cm，因打滑實際只動了 25 cm，補償係數設為 30/25 = 1.2）**：
    ```bash
    python3 move_distance.py 0.3 0.05 1.2
    ```

*註：該腳本會實時讀取里程計 `/base_controller/odom` 並計算已移動的歐氏距離，達到設定目標後會自動發送零速度指令停下小車並關閉節點。當在草地等低摩擦力路面運行時，請善用第三個參數「打滑補償係數」來修正輪胎打滑造成的誤差。*