ubuntu 指令
usb名稱查詢  ll /dev/ 
usb裝置列表 lsusb
更改裝置名稱
sudo ./create_usb_rule.sh </dev/ttyUSB0> <usb_custom_name>
顯示所有檔案
ls -a

在wildbot_workspace啟動車子

啟動車子
sudo ./scripts/00_start_all.sh
在wildbot_workspace啟動docker
啟動docker
sudo ./launch_shell.sh
進入docker
sudo docker exec -it wildbot bash
sudo docker exec -it compose-kros_car-1 bash
關閉 docker
exit
sudo docker compose down
讀取實時參數：ros2 topic echo /joint_states

底盤移動：
ros2 topic pub -r 10 /base_controller/cmd_vel geometry_msgs/msg/TwistStamped \
"{header: {frame_id: 'base_link'}, twist: {linear: {x: 0.2}, angular: {z: 0.0}}}"
最高速：10.844

執行imu
要開兩個視窗第一個啟動
source /opt/ros/jazzy/setup.bash
source /workspaces/imu_ws/install/setup.bash
ros2 run handsfree_imu_ros2 imu_a9_node --ros-args \
  -p port:=/dev/imu_a9 \
  -p baud:=921600
第二個讀參數
sudo docker exec -it wildbot bash

source /opt/ros/jazzy/setup.bash
source /workspaces/imu_ws/install/setup.bash
ros2 topic echo /imu/rpy_deg
也可以第二個輸入這行
ros2 topic echo /imu/rpy_deg

如果之後要在其他 ROS2 Python 程式裡使用，就訂閱：
topic: /imu/rpy_deg
type: geometry_msgs/msg/Vector3


一個校正車頭程式
ros2 run wildbot_control yaw_hold_node --ros-args \
  -p linear_x:=0.0 \
  -p kp:=0.02 \
  -p deadband_deg:=1.5 \
  -p max_angular_z:=0.3

xbox遙控讀數值要開兩個頁面
先
sudo docker exec -it compose-kros_car-1 bash
第一頁面
pkill -f game_controller_node
source /opt/ros/jazzy/setup.bash
ros2 run joy joy_node --ros-args \
  -p dev:=/dev/input/js0 \
  -p deadzone:=0.01 \
  -p autorepeat_rate:=20.0
第二頁面
source /opt/ros/jazzy/setup.bash
ros2 topic echo /joy

開機啟動車子去遙控拍照流程
啟動容器
cd ~/桌面/wildbot_workspace-main
sudo docker compose -f docker/compose/docker-compose_kros_car.yml up -d

開一個終端機啟動底層（在workspace.main裡面
sudo ./scripts/00_start_all.sh

確認底盤有成功啟動
sudo docker logs compose_kros_car --tail 80
再確認 topic：
sudo docker exec -it wildbot bash -lc "source /opt/ros/jazzy/setup.bash && ros2 topic list | grep -Ei 'cmd|vel|base|controller'"
（正常要看到）
/base_controller/cmd_vel
/base_controller/odom

啟動腳本
sudo ./launch_shell_all.sh
進入docker
sudo docker exec -it wildbot bash
sudo docker exec -it compose-kros_car-1 bash
正式遙控 + 拍照
接下來需要開 兩個終端機。
（以下在docker執行）
第一個終端機
source /opt/ros/jazzy/setup.bash
ros2 run joy joy_node --ros-args -p dev:=/dev/input/raikiri

第二個終端機
source /opt/ros/jazzy/setup.bash
cd /workspaces
python3 joy_to_base_camera.py

手臂啟動
有機會docker李沒有ros2control 要安裝
apt update
apt install -y ros-jazzy-ros2controlcli

