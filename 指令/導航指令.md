
cd "/home/robot/wildbot_real car/wildbot_workspace-main" &&  sudo ./scripts/00_start_all.sh

# 1. 啟動所有硬體驅動 (主機端 Terminal 1)
cd "/home/robot/wildbot_real car/wildbot_workspace-main"
sudo ./scripts/00_start_all.sh

# 2. 啟動 Nav2 + YOLO 3D (主機端 Terminal 2)
cd "/home/robot/wildbot_real car/wildbot_workspace-main" && ./quick_start.sh

    source install/setup.bash
    ros2 topic pub -1 /initialpose geometry_msgs/msg/PoseWithCovarianceStamped '{header: {frame_id: "map"}, pose: {pose: {position: {x: 0.0, y: 0.0, z: 0.0}, orientation: {x: 0.0, y: 0.0, z: 0.0, w: 1.0}}}}'


# 停止所有背景服務 (主機端 Terminal 2)
./quick_start.sh stop


# 4. 啟動任務協調器 (容器內 Terminal 3)
docker exec -it -w /workspaces/workspaces/src/arm_ik/arm_ik/ compose-kros_car-1 bash

source /workspaces/install/setup.bash

# 執行任務協調器 (預設標籤為 'bear')
python3 competition_coordinator.py

# 執行任務協調器 (自訂參數：調整距離與打滑補償)
python3 competition_coordinator.py 0.38 1.0

# 5. 執行手動導航測試 (容器內 Terminal 3)
# 導航至點位 1
python3 nav_execute.py 1

# 依序執行導航 (1 -> 2 -> 1)
python3 nav_execute.py 1 2 1
