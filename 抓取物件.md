# 🤖 Wildbot YOLO 3D 偵測與手臂抓取操作指南

本指南說明如何啟動 YOLO 3D 視覺辨識，並結合手臂實時座標監控、手臂點位教導與執行，以達成自動化物件辨識與抓取任務。

---

## 1. 啟動機器人基礎服務
在主機開啟終端機，啟動底盤、相機、雷達與 Bridge 等基礎服務：
```bash
cd "/home/robot/wildbot_real car/wildbot_workspace-main"
sudo ./scripts/00_start_all.sh
```

---

## 2. 啟動 YOLO 3D 偵測
另開一個終端機，進入執行中的主容器，並執行 YOLO 3D 偵測腳本。
這會啟動 3D 深度物體檢測，並將偵測結果發布至 `/yolo/detections_3d` 話題。
```bash
# 進入 kros_car 容器
docker exec -it compose-kros_car-1 bash

# 啟動 YOLO 3D
./yolo_3d.sh
```
> [!NOTE]
> YOLO 3D 模式需要深度相機發布 `/camera/depth/image_raw`，且會自動激活 `/yolo/yolo_node`、`/yolo/detect_3d_node` 與 `/yolo/debug_node` 節點。

---

## 3. YOLO 自動對齊、距離補償與自動夾取模式 (`camera and grab.py`)
在另一個終端機中進入容器，執行 `camera and grab.py`。
此腳本在**不指定點位序列**執行時，會自動執行以下完整的自動對齊與夾取流程：
1. **手臂初始化**：自動移動至第一點位（slot `"1"`，預備/觀察點位）。
2. **等待 YOLO 3D 偵測**：持續讀取 `/yolo/detections_3d` 直到發現信心度 $\ge 0.7$ 的目標物。
3. **差距計算與對齊**：計算目標物 X 軸位置（距前擋板距離）與夾爪中心 X 軸位置的**差距值 (diff)**，並自動控制底盤前進或後退該差距距離（已整合打滑補償）。
4. **自動執行夾取 (Slot 2)**：底盤對齊移動完成後，手臂會**自動移動至第二點位 (slot `"2"`，夾取點位)** 並閉合夾爪。
5. **防燒毀安全退回**：夾爪閉合後等待 0.5 秒，會**自動釋放退回 2° (0.035 rad)**，以釋放馬達持續堵轉的壓力，隨後安全關閉程式。

```bash
# 進入容器
docker exec -it compose-kros_car-1 bash

# 進入手臂 ik 控制目錄
cd /workspaces/workspaces/src/arm_ik/arm_ik/

# 執行自動對齊（草地預設補償係數為 1.29）
python3 "camera and grab.py"

# 若在一般路面（無打滑）執行，可手動指定補償係數為 1.0：
python3 "camera and grab.py" 1.0
```

### 💡 實時監看終端機輸出資訊：
執行過程中，終端機會顯示當前鎖定之目標物資訊、計算出的 X 軸差距值、已前進里程以及補償後的目標移動距離。

---

## 4. 手臂動作點位教導與序列執行

由於目前自動逆運動學 (IK) 抓取改為手動教導與精確回放模式，請依照以下步驟進行動作規劃：

### A. 錄製/教導動作點位 (`grab_teach.py`)
使用手把將手臂移至特定姿勢（例如：預備位、抓取壓實位、夾爪閉合位、抬起運送位），並使用鍵盤紀錄點位。
```bash
# 進入容器並切換至目錄
docker exec -it compose-kros_car-1 bash
cd /workspaces/workspaces/src/arm_ik/arm_ik/

# 啟動教導程式
python3 grab_teach.py
```
*   **按 `0` ~ `9`**：於【紀錄模式】下儲存當前姿態至 `arm_poses.json`。
*   **按 `R`**：切換模式。切換至【回放模式】後，按數字鍵會控制手臂自動移至該點位。
*   **按 `I`**：印出目前手臂與夾爪的實時空間座標 (X, Z cm) 及馬達溫度。
*   **按 `X`**：退出程式。

### B. 執行已儲存的動作序列
教導點位存入 `arm_poses.json` 後，您可以使用 `camera and grab.py` 或 `grab_execute.py` 傳入參數，按順序執行移動（例如：先移動到點位 1，再移動到點位 2，最後回到點位 1）：

**使用 `camera and grab.py` 執行序列：**
```bash
python3 "camera and grab.py" 1 2 1
```

**使用 `grab_execute.py` 執行序列：**
```bash
python3 grab_execute.py 1 2 1
```
*註：執行後，程式會在非阻塞狀態下 spin 等待，以確保實時 Joint 狀態更新，並在抵達每個點位時於終端機輸出精確的物理座標。*

---

## 5. 安全防護與硬體限制規範 ⚠️

在操作與規劃手臂點位時，必須嚴格遵守以下限制，以防硬體損毀或馬達燒毀：

### 🔒 關節限制與保護
- **第二關節限制 (`arm_1_joint`)**：$30^\circ \sim 210^\circ$（請注意避免碰撞地板）。
- **第三關節限制 (`arm_2_joint`)**：$0^\circ \sim 240^\circ$（請注意避免碰撞地板）。
- **夾爪限制 (`gripper_joint`)**：
  - **全開角度**：$240^\circ$
  - **閉合極限**：$168^\circ$
  - **⚠️ 警告**：夾爪控制角度**嚴禁小於 $168^\circ$**（會造成馬達過夾/堵轉燒毀）。
  - **防燒毀退回機制**：點位教導中，夾爪執行夾緊動作後，應至少退回 **$2^\circ$**（例如夾緊後設定為 $170^\circ$）以釋放持續堵轉壓力。

### 🌡️ 溫度監控與過熱保護
系統運行中會持續讀取並監控 `/arm_joint_temperatures` 話題：
- **警告臨界值 ($65^\circ\text{C}$)**：任一馬達溫度達 $65^\circ\text{C}$ 時，終端機將顯示警告。
- **強制停機臨界值 ($70^\circ\text{C}$)**：任一馬達溫度達 $70^\circ\text{C}$ 以上時，系統會**強制鎖定 (Software E-Stop)** 並攔截所有移動與控制指令，必須等待馬達冷卻至 **$60^\circ\text{C}$ 以下** 才能手動解鎖恢復正常運作。

## 過橋程式

wildbot_workspace-main/workspaces/src/go_bridge